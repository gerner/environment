#!/bin/bash

set -eu
set -o pipefail

FMLLOG=~/.fml

FMLS=0

trap cntrl_c INT

function cntrl_c() {
    echo "`date`	${FMLS}" >> ${FMLLOG}
    exit 0
}


while(true); do
    echo -n 'fml';
    FMLS=$((${FMLS} + 1))
    sleep .1;
done
