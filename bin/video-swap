#!/bin/bash
set -e

LAPTOP="LVDS1"

if xrandr | awk '/^[^ ]/ {lastout=$0} /\*/ {print lastout}' | grep -q -v "$LAPTOP" ; then
    xrandr --output HDMI1 --off --output VGA1 --off --output LVDS1 --auto
else
    xrandr --output LVDS1 --off --output HDMI1 --auto --output VGA1 --auto
fi
