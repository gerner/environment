#!/bin/bash
set -e
set -u
set -o pipefail

LAPTOP="^LVDS1|^eDP1"

if xrandr | awk '/^[^ ]/ {lastout=$0} /\*/ {print lastout}' | egrep -q -v "$LAPTOP" ; then
    video-laptop
else
    video-desktop
fi
